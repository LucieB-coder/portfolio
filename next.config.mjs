/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    swcMinify: true,
    trailingSlash: true, // Ensure URLs are correctly handled with trailing slashes  
};

export default nextConfig;
